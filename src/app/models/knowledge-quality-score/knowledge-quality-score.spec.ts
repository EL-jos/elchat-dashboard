import { KnowledgeQualityScore } from './knowledge-quality-score';

describe('KnowledgeQualityScore', () => {
  it('should create an instance', () => {
    expect(new KnowledgeQualityScore()).toBeTruthy();
  });
});
